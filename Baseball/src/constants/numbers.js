const ZERO = 0;
const ONE = 1;
const THREE = 3;
const START_INCLUSIVE = 1;
const END_INCLUSIVE = 9;
const RESTART = 1;
const EXIT = 2;
const DIGIT = 3;

export { ZERO, ONE, THREE, START_INCLUSIVE, END_INCLUSIVE, RESTART, EXIT, DIGIT };
